# Random-Color-Changer
An interactive mini JavaScript project that dynamically updates the background color with a single click. Users can generate a random color from a predefined palette or apply their own custom color using an input field. The selected color is displayed instantly for better user feedback.
